# 🎴 화도쿠(WhaDoku) - 스도쿠 & 화투 족보 웹 게임

화도쿠는 전통적인 스도쿠 퍼즐에 한국의 화투 족보 시스템을 결합한 웹 게임입니다. 이 버전은 화투패 대신 귀여운 과일 이미지를 사용하여 누구나 쉽게 즐길 수 있도록 제작되었습니다.

플레이어는 스도쿠 규칙에 따라 보드를 채워나가면서, 가로/세로줄 및 3x3 블록에서 화투 족보를 완성하여 점수를 획득하는 것을 목표로 합니다.

## 🚀 실행 방법

이 프로젝트는 별도의 빌드나 설치 과정이 필요 없습니다.

1.  이 프로젝트 폴더를 다운로드합니다.
2.  폴더 안에 있는 `index.html` 파일을 웹 브라우저(Chrome, Edge 등)로 열면 바로 게임을 시작할 수 있습니다.

## ✨ 주요 기능

*   **메인 메뉴**: 게임 시작 및 난이도 선택이 가능한 초기 화면.
*   **난이도 조절**: 초급, 중급, 고급 세 가지 레벨을 제공합니다.
*   **인터랙티브 보드**: 마우스 클릭으로 쉽게 숫자를 입력하고 수정할 수 있습니다.
*   **족보 시스템**: 완성된 라인(가로, 세로, 3x3 박스)을 기반으로 화투 족보 점수를 계산하여 보여줍니다.
*   **안내 기능**: 게임 방법을 알려주는 '안내'와 점수 계산법을 설명하는 '족보' 팝업창을 제공합니다.
*   **사운드 효과**: 버튼 클릭, 에러 발생 등 주요 상호작용에 대한 효과음이 포함되어 있습니다.

## 📂 프로젝트 구조

이 프로젝트는 순수 HTML, CSS, JavaScript로 구성되어 있으며, 각 파일의 역할은 다음과 같습니다.

-   **`index.html`**: 모든 UI 요소가 포함된 기본 HTML 파일입니다.
-   **`package.json`**: 프로젝트 정보와 개발에 사용된 도구(prettier, eslint) 정보가 기록되어 있습니다.

### `src/` - 소스 코드 폴더

-   **`css/`**: UI 스타일을 정의하는 CSS 파일들이 역할별로 분리되어 있습니다.
    -   `base.css`: 기본 스타일 초기화
    -   `layout.css`: 메인 메뉴, 게임 화면 등 전체 레이아웃
    -   `sudoku-board.css`: 스도쿠 보드 스타일
    -   `components.css`: 버튼, 모달 등 공통 컴포넌트 스타일
    -   ... 등
-   **`js/`**: 게임의 모든 로직을 담당하는 JavaScript 파일들이 역할별로 분리되어 있습니다.
    -   `main.js`: 게임을 시작하고 초기화하는 메인 스크립트입니다.
    -   `game.js`: 핵심 게임 로직(보드 생성, 유효성 검사 등)을 담당합니다.
    -   `game-flow.js`: 메인 메뉴에서 게임 화면으로 전환하는 등 전체 게임의 흐름을 관리합니다.
    -   `hanafuda.js`: 족보를 계산하고 점수를 매기는 로직을 담당합니다.
    *   `state.js`: 게임의 현재 상태(예: 선택된 숫자, 보드 데이터)를 관리합니다.
    -   `ui.js`: 화면의 요소를 업데이트하거나 변경하는 UI 관련 로직을 담당합니다.
    -   `events.js`: 사용자의 클릭과 같은 모든 이벤트를 처리합니다.

### `public/` - 정적 자원 폴더

-   **`images/`**: 게임에 사용되는 모든 이미지 파일(화투, 버튼, 랭크 등)이 저장되어 있습니다.
-   **`sounds/`**: 게임 내 효과음 파일이 저장되어 있습니다.
-   **`favicon.png`**: 브라우저 탭에 표시되는 파비콘 이미지입니다.

https://github.com/Rlatjdfyd/whadoku.git