# 🎴 화도쿠(WhaDoku) - 스도쿠 & 화투 족보 웹 게임

화도쿠는 전통적인 스도쿠 퍼즐에 한국의 화투 족보 시스템을 결합한 웹 게임입니다. 이 버전은 화투패 대신 귀여운 과일 이미지를 사용하여 누구나 쉽게 즐길 수 있도록 제작되었습니다.

플레이어는 스도쿠 규칙에 따라 보드를 채워나가면서, 가로/세로줄 및 3x3 블록에서 화투 족보를 완성하여 점수를 획득하는 것을 목표로 합니다.

## 🚀 실행 방법

이 프로젝트는 Vite 기반의 웹 애플리케이션입니다. 개발 환경 설정 및 실행 방법은 다음과 같습니다.

1.  **프로젝트 다운로드**: 이 프로젝트 폴더를 다운로드합니다.
2.  **의존성 설치**: 프로젝트 루트 디렉토리에서 터미널을 열고 다음 명령어를 실행하여 필요한 패키지들을 설치합니다.
    ```bash
    npm install
    ```
3.  **개발 서버 실행**: 다음 명령어를 실행하면 개발 서버가 시작되고, 웹 브라우저에서 게임을 플레이할 수 있습니다. 코드를 수정하면 자동으로 새로고침됩니다.
    ```bash
    npm run dev
    ```
4.  **배포용 빌드**: 프로덕션 환경에 배포하기 위한 최적화된 파일을 생성하려면 다음 명령어를 실행합니다. `dist` 폴더에 빌드 결과물이 생성됩니다.
    ```bash
    npm run build
    ```

## ✨ 주요 기능

*   **메인 메뉴**: 게임 시작 및 난이도/주제 선택이 가능한 초기 화면.
*   **난이도 조절 및 기억**: 초급, 중급, 고급, **랜덤** 네 가지 레벨을 제공합니다. 사용자가 마지막에 선택한 난이도를 기억하여 재접속 시 자동으로 설정됩니다.
*   **여정 모드 (스테이지 맵)**: 각 난이도별로 단계별 스테이지를 클리어해 나가는 모드입니다. 81개 단위의 스테이지 맵을 제공하며, 클리어 상태에 따라 스테이지가 해금되는 진행 방식을 가집니다.
*   **PWA 및 전체 화면 지원**: 모바일 브라우저에서 '홈 화면에 추가'를 통해 설치하면 주소창 없는 전체 화면으로 게임을 즐길 수 있습니다.
*   **인터랙티브 보드**: 마우스 클릭으로 쉽게 숫자를 입력하고 수정할 수 있습니다.
*   **족보 시스템**: 완성된 라인(가로, 세로, 3x3 블록)을 기반으로 화투 족보 점수를 계산하여 보여줍니다.
*   **상단 글귀 표시 제어**: '랜덤' 난이도를 제외한 다른 난이도에서는 게임 화면 상단에 글귀가 표시되지 않습니다.
*   **저장된 글귀 컬렉션**: 메인 메뉴의 '달성' 블록을 통해 접근 가능하며, 주제별 수집 진행률을 시각적으로 확인할 수 있습니다.
*   **안내 및 블로그 연결**: 게임 방법, 족보, 기록 등을 모달로 제공하며, 메인 메뉴의 '종료' 버튼을 통해 제작자의 블로그로 연결됩니다.
*   **사운드 효과**: 버튼 클릭, 에러 발생 등 주요 상호작용에 대한 효과음이 포함되어 있습니다.

## 📂 프로젝트 구조

이 프로젝트는 Vite를 사용하여 개발되었으며, 순수 HTML, CSS, JavaScript로 구성되어 있습니다.

-   **`index.html`**: 모든 UI 요소가 포함된 기본 HTML 파일입니다. PWA 설정을 위한 매니페스트 연결 및 서비스 워커 등록 스크립트가 포함되어 있습니다.
-   **`public/`**: 정적 자원 및 PWA 핵심 파일
    -   `manifest.json`: 앱의 이름, 아이콘, 전체 화면(standalone) 모드 설정 파일.
    -   `sw.js`: 오프라인 대응 및 앱 설치 지원을 위한 서비스 워커 파일.
    -   `images/`, `sounds/`, `data/passages/`: 게임 자원 및 데이터 파일.

### `src/` - 소스 코드 폴더

-   **`css/`**: UI 스타일 정의 파일들.
    -   `layout.css`: 메인 메뉴 및 스테이지 맵 모드 전용 스타일 포함.
    -   `sudoku-board.css`: 스도쿠 보드 및 스테이지 맵(잠김, 열림, 완료 상태) 스타일.
-   **`js/`**: 게임 로직 파일들.
    -   `game-flow.js`: 스테이지 선택 모드(`enterStageMode`) 및 진행 상황 업데이트 로직.
    -   `state.js`: 게임 상태 관리. **마지막 선택 난이도 기억 기능 및 스테이지 진행도 로직이 포함되어 있습니다.**
    -   `ui.js`: 81개 단위 가상 페이지네이션 스테이지 맵 렌더링.
    -   **`events/`**: 분리된 이벤트 처리 로직.
        -   `mainMenuEvents.js`: 난이도 선택 시 자동 저장 및 **종료 버튼 클릭 시 제작자 블로그 연결 로직**이 구현되어 있습니다.

## 📝 향후 계획 및 개선 방안

*   **성능 최적화**: 로딩 속도 및 자원 활용 최적화.
*   **UI/UX 개선**: 스테이지 클리어 시 시각적 피드백(애니메이션) 강화.
*   **콘텐츠 확장**: 스테이지별 고유 보상 및 새로운 족보 시스템 추가.
*   **완전한 PWA 구현**: 오프라인 모드에서의 완벽한 플레이 지원을 위한 캐싱 전략 고도화.
